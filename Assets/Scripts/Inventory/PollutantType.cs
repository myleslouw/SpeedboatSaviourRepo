using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PollutantType : MonoBehaviour
{
    public enum type {Glass, Plastic, GeneralWaste};

    public enum subType { bottles, bags, cans, trash, takeaway, coffee }

}
